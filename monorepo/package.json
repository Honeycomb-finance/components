{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "dev:app": "turbo run dev:app --concurrency=30",
    "dev:packages": "turbo run dev --concurrency=30",
    "start:app": "turbo run start --filter=example-app",
    "start:wallet": "turbo run start --filter=walletmodal-example-app",
    "build:packages": "turbo run build --filter=@pangolindex/* --filter=@honeycomb/*",
    "postinstall": "patch-package"
  },
  "devDependencies": {
    "@pangolindex/sdk": "5.3.5-internal.92f6b5a",
    "eslint": "^7.32.0",
    "eslint-config-custom": "*",
    "patch-package": "6.4.7",
    "prettier": "^2.5.1",
    "turbo": "latest",
    "tsc-alias": "1.8.7"
  },
  "peerDependencies": {
    "@pangolindex/sdk": ">=2.0.1"
  }
}
